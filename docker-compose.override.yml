version: '3.8'

services:

  backend:
    environment:
      - DJANGO_DEBUG=1
    command:
      [ "python", "manage.py", "runserver", "0.0.0.0:8000" ]
    ports:
      - 8000:8000

  frontend:
    command:
      [ "npm", "run", "dev" ]
    ports:
      - 3000:3000
